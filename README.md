# ZooMarket

> This app uses Node, Express, Sequalize (Postgres).

## Quick Start

``` bash
# Install dependencies
npm install

# Server on localhost:3000
npm start

# Dev Server (Nodemon)
npm run dev
